# SwiftPM
.build/
.swiftpm/
Package.resolved

# macOS
.DS_Store

# Build artifacts
Bomi.app/
*.dSYM/

# Xcode (not used, but just in case)
xcuserdata/
*.xcworkspace

# SDD scratch
.superpowers/

# devmode hook logs + session backups (runtime noise)
.claude/logs/
.claude/backups/
# devmode: session snapshots are runtime state, never tracked (snapshot-untrack)
.claude/sessions/
